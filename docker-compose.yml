services:
  daigestr:
    build:
      context: ./mcp
      args:
        DAIGESTR_VERSION: ${DAIGESTR_VERSION:-dev}
    container_name: daigestr
    restart: unless-stopped
    env_file: .env
    # Ports in docker-compose.override.yml (localhost-only)
    volumes:
      - ./data:/data
